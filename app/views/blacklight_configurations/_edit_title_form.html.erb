<%= form_for @blacklight_configuration, url: { action: :update_title } do |f| %>
  <% if @blacklight_configuration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blacklight_configuration.errors.count, "error") %> prohibited this blacklight_configuration from being saved:</h2>

      <ul>
      <% @blacklight_configuration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <strong>Configuration:</strong>
    <%= @blacklight_configuration.configuration %>
  </div>

  <div class="field">
    <%= f.label "Field" %><br>
    <%= collection_select(:blacklight_configuration, :solr_field, @title_compatible_fields, :first, :last) %>  
  </div>
  <p>&nbsp;</p>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
