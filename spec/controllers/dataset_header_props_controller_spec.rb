require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe DatasetHeaderPropsController do

  # This should return the minimal set of attributes required to create a valid
  # DatasetHeaderProp. As you add validations to DatasetHeaderProp, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) { { "name" => "MyString" } }

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # DatasetHeaderPropsController. Be sure to keep this updated too.
  let(:valid_session) { {} }

  describe "GET index" do
    it "assigns all dataset_header_props as @dataset_header_props", pending: true do
      dataset_header_prop = DatasetHeaderProp.create! valid_attributes
      get :index, {}, valid_session
      assigns(:dataset_header_props).should eq([dataset_header_prop])
    end
  end

  describe "GET show" do
    it "assigns the requested dataset_header_prop as @dataset_header_prop", pending: true do
      dataset_header_prop = DatasetHeaderProp.create! valid_attributes
      get :show, {:id => dataset_header_prop.to_param}, valid_session
      assigns(:dataset_header_prop).should eq(dataset_header_prop)
    end
  end

  describe "GET new" do
    it "assigns a new dataset_header_prop as @dataset_header_prop", pending: true do
      get :new, {}, valid_session
      assigns(:dataset_header_prop).should be_a_new(DatasetHeaderProp)
    end
  end

  describe "GET edit" do
    it "assigns the requested dataset_header_prop as @dataset_header_prop", pending: true do
      dataset_header_prop = DatasetHeaderProp.create! valid_attributes
      get :edit, {:id => dataset_header_prop.to_param}, valid_session
      assigns(:dataset_header_prop).should eq(dataset_header_prop)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new DatasetHeaderProp", pending: true do
        expect {
          post :create, {:dataset_header_prop => valid_attributes}, valid_session
        }.to change(DatasetHeaderProp, :count).by(1)
      end

      it "assigns a newly created dataset_header_prop as @dataset_header_prop", pending: true do
        post :create, {:dataset_header_prop => valid_attributes}, valid_session
        assigns(:dataset_header_prop).should be_a(DatasetHeaderProp)
        assigns(:dataset_header_prop).should be_persisted
      end

      it "redirects to the created dataset_header_prop", pending: true do
        post :create, {:dataset_header_prop => valid_attributes}, valid_session
        response.should redirect_to(DatasetHeaderProp.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved dataset_header_prop as @dataset_header_prop", pending: true do
        # Trigger the behavior that occurs when invalid params are submitted
        DatasetHeaderProp.any_instance.stub(:save).and_return(false)
        post :create, {:dataset_header_prop => { "name" => "invalid value" }}, valid_session
        assigns(:dataset_header_prop).should be_a_new(DatasetHeaderProp)
      end

      it "re-renders the 'new' template", pending: true do
        # Trigger the behavior that occurs when invalid params are submitted
        DatasetHeaderProp.any_instance.stub(:save).and_return(false)
        post :create, {:dataset_header_prop => { "name" => "invalid value" }}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested dataset_header_prop", pending: true do
        dataset_header_prop = DatasetHeaderProp.create! valid_attributes
        # Assuming there are no other dataset_header_props in the database, this
        # specifies that the DatasetHeaderProp created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        DatasetHeaderProp.any_instance.should_receive(:update).with({ "name" => "MyString" })
        put :update, {:id => dataset_header_prop.to_param, :dataset_header_prop => { "name" => "MyString" }}, valid_session
      end

      it "assigns the requested dataset_header_prop as @dataset_header_prop", pending: true do
        dataset_header_prop = DatasetHeaderProp.create! valid_attributes
        put :update, {:id => dataset_header_prop.to_param, :dataset_header_prop => valid_attributes}, valid_session
        assigns(:dataset_header_prop).should eq(dataset_header_prop)
      end

      it "redirects to the dataset_header_prop", pending: true do
        dataset_header_prop = DatasetHeaderProp.create! valid_attributes
        put :update, {:id => dataset_header_prop.to_param, :dataset_header_prop => valid_attributes}, valid_session
        response.should redirect_to(dataset_header_prop)
      end
    end

    describe "with invalid params" do
      it "assigns the dataset_header_prop as @dataset_header_prop", pending: true do
        dataset_header_prop = DatasetHeaderProp.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        DatasetHeaderProp.any_instance.stub(:save).and_return(false)
        put :update, {:id => dataset_header_prop.to_param, :dataset_header_prop => { "name" => "invalid value" }}, valid_session
        assigns(:dataset_header_prop).should eq(dataset_header_prop)
      end

      it "re-renders the 'edit' template", pending: true do
        dataset_header_prop = DatasetHeaderProp.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        DatasetHeaderProp.any_instance.stub(:save).and_return(false)
        put :update, {:id => dataset_header_prop.to_param, :dataset_header_prop => { "name" => "invalid value" }}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested dataset_header_prop", pending: true do
      dataset_header_prop = DatasetHeaderProp.create! valid_attributes
      expect {
        delete :destroy, {:id => dataset_header_prop.to_param}, valid_session
      }.to change(DatasetHeaderProp, :count).by(-1)
    end

    it "redirects to the dataset_header_props list", pending: true do
      dataset_header_prop = DatasetHeaderProp.create! valid_attributes
      delete :destroy, {:id => dataset_header_prop.to_param}, valid_session
      response.should redirect_to(dataset_header_props_url)
    end
  end

end
